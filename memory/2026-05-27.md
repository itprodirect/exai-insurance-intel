# Memory: Exa modernization status and live grounding validation

## Objective

Sync repo docs/status after the Exa 2026 modernization, cost-model reconciliation, and bounded live grounding validation.

## Changes made

- Updated the 2026-05-27 live grounding session note to record the successful rerun after the placeholder Exa API key was replaced.
- Reconciled roadmap and issue-tracker status so `#51` and `#56` are closed/completed instead of future follow-up work.
- Updated ADR-0002 and validation boundary docs to distinguish smoke validation from the bounded live CLI rerun.
- Kept the status sync docs-only; no app code, tests, frontend, persistence, deployment, or live Exa calls were changed or run during the sync.

## Live validation evidence recorded

- Live `research` artifact: `request_id=7f2b06e5b419f4ca182d1144c0c9e4d9`, `actual_cost_usd=0.015`, 5 results, no `output.content`, no `output.grounding`, `grounding_count=0`.
- Live `structured-search` artifact: `request_id=c4e750dd01ad3bb1ce78621663609d59`, `actual_cost_usd=0.012`, `output.content`, `output.grounding`, `grounding_count=26`.
- Structured `report.md` rendered `Grounding / Source Review`.

## Validation run

- `python -m ruff check .` -> passed.
- `python -m pytest -q` -> passed (`299 passed`, one existing Jupyter path deprecation warning).
- `python scripts/run_live_validation.py --mode smoke` -> passed and wrote smoke artifacts under `live-validation-artifacts/`.
- `git diff --check` -> passed.

## Boundaries

- The bounded live evidence covers CLI `research` and `structured-search` only.
- The local UI validation path remains smoke/mock.
- S3, Postgres, frontend live mode, deployment, monitoring, MCP, agent, and broad production validation were not claimed.

## Next thin slice

- Continue Phase 5 Level 1 persistence work without widening into deployment or broad infra redesign.
