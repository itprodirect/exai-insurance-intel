# Backend API URL (server-side only, not exposed to browser)
BACKEND_URL=http://127.0.0.1:8000

# Backend API key for authenticated requests (server-side only).
# Leave blank to skip auth (matches backend behavior when PILOT_API_KEY is unset).
BACKEND_API_KEY=
